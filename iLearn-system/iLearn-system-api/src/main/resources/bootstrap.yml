server:
  servlet:
    context-path: /system
  port: 63110
#微服务配置
spring:
  application:
    name: ilearn-system-service

# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml

# swagger 文档配置
swagger:
  title: "数据字典"
  description: "数据字典相关数据"
  base-package: com.ilearn.system.controller
  enabled: true
  version: 1.0.0
