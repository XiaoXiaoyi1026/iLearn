# 应用名称
spring:
  application:
    name: iLearn-content-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///ilearn_content?serviceTimezone=UTC&userUnicode=true&useSSL=false&
    username: root
    password: XYX1026kwi!!

# 配置日志文件路径
logging:
  config: classpath:log4j2-dev.xml